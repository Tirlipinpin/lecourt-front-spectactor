version: '3'
services:
  front:
    build:
      context: .
      dockerfile: development.Dockerfile
    ports:
      - 3000:3000
    environment:
      - VIRTUAL_HOST=front.docker
      - VIRTUAL_PORT=3000
    volumes:
      - .:/app
